//
//  SmartGreetingBar.swift
//  StreakSync
//
//  Clean, simple contextual greeting
//

import SwiftUI

// Deprecated: SmartGreetingBar (removed from dashboard). Keeping a no-op placeholder to avoid compile errors if referenced elsewhere.
struct SmartGreetingBar: View {
    let userName: String
    var body: some View { EmptyView() }
}
